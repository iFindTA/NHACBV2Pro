CREATE TABLE IF NOT EXISTS t_usrs (
uid TEXT NULL,
account TEXT DEFAULT NULL,
acctype TEXT DEFAULT 'normal',
passwd TEXT DEFAULT NULL,
authorToken TEXT DEFAULT NULL,
gender TEXT DEFAULT 0,
level TEXT DEFAULT 0,
nick TEXT DEFAULT NULL,
avatar TEXT DEFAULT NULL,
signature TEXT DEFAULT NULL,
authorName TEXT DEFAULT NULL,
authorID TEXT DEFAULT NULL,
authorType TEXT DEFAULT 0,
email TEXT DEFAULT NULL,
postal TEXT DEFAULT NULL,
state TEXT DEFAULT '86',
province TEXT DEFAULT NULL,
city TEXT DEFAULT NULL,
logdate TEXT DEFAULT NULL,
overage TEXT DEFAULT 0,
allIncome TEXT DEFAULT 0,
numGolden TEXT DEFAULT 0,
points TEXT DEFAULT 0,
riskCapital TEXT DEFAULT 0,
numBankCard TEXT DEFAULT 0,
accWeiXin TEXT DEFAULT NULL,
accAlipay TEXT DEFAULT NULL,
ext TEXT DEFAULT NULL,
PRIMARY KEY (uid)
)|
CREATE TABLE IF NOT EXISTS t_ads (
adid TEXT NULL,
img TEXT DEFAULT NULL,
link TEXT DEFAULT NULL,
start TEXT DEFAULT NULL,
end TEXT DEFAULT NULL,
type TEXT DEFAULT NULL,
distype TEXT DEFAULT 0,
download TEXT DEFAULT 0,
discount TEXT DEFAULT 0,
ext TEXT DEFAULT NULL,
PRIMARY KEY (adid)
)|
CREATE TABLE IF NOT EXISTS t_app_setting (
appversion TEXT NULL,
reviewed TEXT DEFAULT 0,
guideFlashed TEXT DEFAULT 0,
ext TEXT DEFAULT NULL,
PRIMARY KEY (appversion)
)|
CREATE TABLE IF NOT EXISTS t_daily_sign (
day TEXT NULL,
sign TEXT DEFAULT 0,
flash TEXT DEFAULT 0,
PRIMARY KEY (day)
)|CREATE TABLE IF NOT EXISTS t_banner (
id TEXT NULL,
news TEXT DEFAULT 0,
zixun TEXT DEFAULT 0,
info TEXT DEFAULT 0,
bannerList TEXT DEFAULT NULL,
newsList TEXT DEFAULT NULL,
PRIMARY KEY (id)
)|CREATE TABLE IF NOT EXISTS t_plat_activity (
platid TEXT NULL,
name TEXT DEFAULT NULL,
icon TEXT DEFAULT NULL,
activities TEXT DEFAULT NULL,
PRIMARY KEY (platid)
)